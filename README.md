# Multi-ModelRoutingSystem
Designed and implemented an adaptive inference routing system that dynamically selects different language models based on prompt complexity in order to balance inference latency, compute cost, and model capability.
